#include "main.h"

/**
 * _abs - computes the absolute value of an integer.
 *
 * @n: is an integer
 *
 * Return: integer
 */

int _abs(int)
{
	int n;

	if (n >= 0)
	{
		return (n);
	}
	else (n < 0);
	{
		return (-n);
	}
}
